add wifi reconnect ...

in settime.py at line #20 

    try:
        s.connect(addr)
    except OSError:
        pass

add lines after except OSerror 

    try:
        s.connect(addr)
    except OSError:
        #trying to reconnect wifi
        import wifi_connect
        wifi_connect.do_connect()
        pass


#j'ai du trouble avec la pin5. j'ai changé pour la 18. peut-être mettre un resistance au lieu de pullip interne